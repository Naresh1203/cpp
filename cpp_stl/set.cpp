#include<bits/stdc++.h>
using namespace std;
{
    void explainSet() //store in sorted manner and without repetation
    {
        set<int> st;
        st.insert(1);
        st.emplace(2);
        st.insert(3;
        st.insert)
        
    }
}